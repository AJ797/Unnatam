---
name: User profile
description: Researcher building novel LLM architectures, AhamV2 author, paper-oriented
type: user
---

The user is an ML researcher who builds novel LLM architectures with the explicit goal of producing papers. Prior project AhamV2 (a representation-engineering / inference-time-steering system on Llama-70B) lives at E:\empire\AhamV2.

Working style:
- Speaks fluently in modern LLM architecture terms (MQA, SSM, MoE, repeng, contrast pairs, residual stream injection). Frame technical discussion at peer level — no need to explain fundamentals.
- Decisive — picks design tradeoffs quickly when given clear options + your lean. Surface 2–3 concrete options with a recommendation rather than open-ended brainstorming.
- Action-oriented — once design is settled they want to start coding. Don't over-spec or over-document.
- Hardware-realistic — aware of VRAM constraints and willing to defer scope (e.g. pulled nested MoE from v1 because it doesn't pay off at small scale).
- Prefers clean, debuggable, portable code (Python, Windows host) over kernel-bound libraries; "civilized" was their word.

Preferences:
- Casual register is fine in conversation; code itself stays clean and conventional.
- Wants thoughts/pushback, not just agreement — ask "what's your intuition" style questions when there's a real design choice.
